#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../docpad/bin/docpad" "$@"
  ret=$?
else 
  node  "$basedir/../docpad/bin/docpad" "$@"
  ret=$?
fi
exit $ret
