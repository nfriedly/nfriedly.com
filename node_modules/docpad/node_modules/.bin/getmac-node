#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../getmac/bin/getmac-node" "$@"
  ret=$?
else 
  node  "$basedir/../getmac/bin/getmac-node" "$@"
  ret=$?
fi
exit $ret
